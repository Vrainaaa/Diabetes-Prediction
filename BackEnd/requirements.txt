seaborn==0.11.2          # Latest stable version compatible with modern environments
Flask==2.0.1             # Compatible and more secure than 1.0.2
matplotlib==3.3.4        # Stable version with fewer dependency issues
pandas==1.3.3            # Compatible version, avoids metadata issues
numpy==1.21.2            # Matches other dependencies
scikit-learn==0.24.2     # Compatible with updated numpy and pandas
streamlit==1.19.0        # If this works for your app, keep it
